[project]
name = "client"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "kokoro-onnx>=0.5.0",
    "opencv-python>=4.13.0.92",
    "soundfile>=0.13.1",
]

[dependency-groups]
dev = [
    "pillow>=12.2.0",
    "pytest>=9.0.3",
]
